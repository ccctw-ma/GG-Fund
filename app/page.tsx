export default function HomePage() {
  return (
    <main className="shell">
      <h1>GG Fund foundation</h1>
      <p>
        Minimal Next.js scaffolding is in place so the build and OpenNext pipeline can run
        before the main Vite experience is migrated.
      </p>
    </main>
  );
}
